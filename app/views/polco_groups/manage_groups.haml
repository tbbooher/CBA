- title "manage groups"
= simple_form_for :user, :url => update_groups_url do |f|
  %div
    %h2 Joined Groups
    = text_area_tag 'joined_groups', nil, "data-pre" => @joined_groups_json_data
  %div
    %h2 Followed Groups
    = text_area_tag 'followed_groups', nil, "data-pre" => @followed_groups_json_data

  %div
    %p
      link to the create group. I think this page would work best with
      three text fields per section for the follow, states, districts and custom
      the join groups would only have one field.
    %p
      = link_to "Create your own custom group", new_polco_group_path
      i'll try to get the create custom group done tonight
  = f.submit
