<h1>Bills Admin View</h1>
<%# javascript 'endless_page' %>

  <%= will_paginate @bills %>

<% @bills.each do |bill| %>
    <h3><%= bill.short_title || bill.official_title %></h3>
    <p> by <%= bill.sponsor.full_title %><br>
      <b>Last action on <%= bill.last_action_on.to_s %>:</b> <%= bill.last_action_text %></p>

    <p><%= link_to 'Show', bill %> |
    <%= link_to 'Edit', edit_bill_path(bill) %> |
    <%= link_to 'Destroy', bill, :confirm => 'Are you sure?', :method => :delete %></p>
    <hr>
<% end %>

  <%= will_paginate @bills %>

<br>

<%= link_to 'New Bill', new_bill_path %>
